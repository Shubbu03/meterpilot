# MeterPilot worker

The Bun process that will claim and execute durable PostgreSQL jobs. Install dependencies from the repository root.

## Commands

```bash
bun run dev
bun run typecheck
bun run test
bun run build
```
