# MeterPilot web

The React and Vite operator dashboard, styled with Tailwind CSS v4 and CSS-first design tokens.

## Commands

```bash
bun run dev
bun run typecheck
bun run test
bun run build
```

Run installation and repository-wide checks from the monorepo root. UI verification is performed manually by the project owner.
